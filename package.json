{
  "name": "bestie",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "lint": "node scripts/check-static.mjs",
    "build": "node scripts/build.mjs"
  }
}
